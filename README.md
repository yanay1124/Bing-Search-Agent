# search
一個由Python寫成，透過本地瀏覽器搜尋並透過lm-studio請求llm整理資訊的搜尋工具，可讓AI Agent請求搜尋資訊的工具。

使用前需安裝以下套件:

```pip install fastapi uvicorn playwright beautifulsoup4 openai python-dotenv```

並輸入`playwright install`進行初始化
